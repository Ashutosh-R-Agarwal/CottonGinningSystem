CREATE TABLE  "PURCHASE_QTY" 
   (	"PO_NO" NUMBER(5,0), 
	"P_QTY" NUMBER(10,0) NOT NULL ENABLE, 
	"P_RATE" NUMBER(10,0) NOT NULL ENABLE, 
	"P_VARIETY" VARCHAR2(10) NOT NULL ENABLE, 
	"RATE_QUINTAL" NUMBER(5,0), 
	 FOREIGN KEY ("PO_NO")
	  REFERENCES  "PURCHASEORDER" ("PONO") ENABLE
   )
