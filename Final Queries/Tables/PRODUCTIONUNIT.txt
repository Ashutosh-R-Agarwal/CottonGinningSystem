CREATE TABLE  "PRODUCTIONUNIT" 
   (	"P_ID" NUMBER(5,0), 
	"P_TRASHQTY" NUMBER(10,0), 
	"P_SEEDQTY" NUMBER(10,0), 
	"P_LINTQTY" NUMBER(10,0), 
	"STOCK_ID" NUMBER(5,0), 
	 PRIMARY KEY ("P_ID") ENABLE, 
	 CONSTRAINT "P_TOTAL" FOREIGN KEY ("STOCK_ID")
	  REFERENCES  "TOTALOUTPUTQTY" ("STOCK_ID") ENABLE
   )
