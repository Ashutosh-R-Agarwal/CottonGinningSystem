CREATE TABLE  "PURCHASEORDER" 
   (	"PONO" NUMBER(5,0), 
	"P_DAY" NUMBER(2,0) NOT NULL ENABLE, 
	"P_MONTH" VARCHAR2(12) NOT NULL ENABLE, 
	"P_YEAR" NUMBER(4,0) NOT NULL ENABLE, 
	"P_TOTAMT" NUMBER(10,0), 
	"E_ID" NUMBER(5,0), 
	"PENDING" NUMBER(1,0) DEFAULT 0, 
	 PRIMARY KEY ("PONO") ENABLE, 
	 FOREIGN KEY ("E_ID")
	  REFERENCES  "EMPLOYEE" ("E_ID") ENABLE
   )
