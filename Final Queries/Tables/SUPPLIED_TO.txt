CREATE TABLE  "SUPPLIED_TO" 
   (	"S_DAY" NUMBER(2,0), 
	"S_MONTH" VARCHAR2(12), 
	"S_YEAR" NUMBER(4,0), 
	"S_QTY" NUMBER(10,0), 
	"C_ID" NUMBER(5,0), 
	"P_ID" NUMBER(5,0), 
	 FOREIGN KEY ("C_ID")
	  REFERENCES  "RAWCOTTON" ("C_ID") ENABLE, 
	 FOREIGN KEY ("P_ID")
	  REFERENCES  "PRODUCTIONUNIT" ("P_ID") ENABLE
   )
